# Deprecated Scripts

This directory contains older versions of scripts that have been superseded by newer implementations.

## Moved Scripts

### Feature Extraction Variants
- `extract_features_backed.py` - Basic feature extraction (replaced by `extract_features_enhanced.py`)
- `extract_features_fast.py` - Fast variant (replaced by enhanced version)
- `extract_features_minimal.py` - Minimal variant (replaced by enhanced version)

### Analysis Scripts
- `compute_metrics_minimal.py` - Basic metrics computation (replaced by full version)
- `verify_database_test.py` - Test script for database verification

## Notes

These scripts are kept for reference but should not be used in production. The main `/scripts/` directory contains the current, maintained versions.

For thesis submission, use the scripts in the parent directory, not these deprecated versions.